{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "swc src -d .build",
    "build:prod": "NODE_ENV=prod swc src -d .build",
    "build-dev": "npm run build && NODE_ENV=development node --inspect .build/index.js",
    "dev:api": "nodemon --ext ts --exec \"npm run build-dev\"",
    "start": "npm run build && NODE_ENV=production node .build/index.js"
  },
  "keywords": [],
  "author": "matheusbbarni",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.219",
    "@swc/jest": "^0.2.22",
    "@types/jest": "^28.1.6",
    "@types/node": "^18.6.1",
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "fastify": "^4.3.0"
  }
}
